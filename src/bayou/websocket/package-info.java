/**
 * WebSocket server.
 */
package bayou.websocket;

// the WebSocket prefix is stupidly long.
// we would really like to name our things as WsXxx, e.g. WsServer, WsChannel, WsMessage.
// unfortunately, "WS" is already used for "Web Service", we don't want to create confusions.